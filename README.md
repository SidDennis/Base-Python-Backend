# Base-Python-Backend
Reusable code to make a functional backend with users

to setup:
  1) insert database info into CONSTANTS
  2) change functions as needed

To run server:
  uvicorn main:app --reload